const blueCircle = require('./circle.js');
const blueCircle2 = require('./circle.js');

const blueCircle2 = require('circle');

const circResult = blueCircle.circumference(4);
console.log(
  'The circumference of a circle of radius 4 is',
  circResult
);

// console.log(`The area of a circle of radius 4 is ${circle.area(4)}`);

console.log(`The area of a circle of radius 4 is ${blueCircle.area(4)}`);
